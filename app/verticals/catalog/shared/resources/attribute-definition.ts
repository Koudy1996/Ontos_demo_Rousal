// @generated by OntOS Codesmith Resource v1
// @ontos-resource-owner commerce.catalog
// @ontos-resource-slug attribute-definition
import type { OntosResourceType } from '@app/core-runtime';
import { Schema } from 'effect';

const checkedUuid = Schema.String.check(Schema.isUUID(), Schema.isTrimmed());
const ResourceIdSchema = checkedUuid.pipe(Schema.brand('CatalogAttributeDefinitionResourceId'), Schema.decodeTo(checkedUuid));
const TenantIdSchema = checkedUuid.pipe(Schema.brand('CatalogTenantId'), Schema.decodeTo(checkedUuid));

export const AttributeDefinitionRefSchema = Schema.Struct({
  moduleId: Schema.Literal('commerce.catalog'),
  resourceId: ResourceIdSchema,
  resourceType: Schema.Literal('commerce.catalog.attribute-definition'),
  tenantId: TenantIdSchema,
});
export type AttributeDefinitionRef = typeof AttributeDefinitionRefSchema.Type;

export const attributeDefinitionResourceDescriptor = {
  capabilities: {
    graphVisible: false,
    linkable: false,
    mediaAttachable: false,
    searchable: false,
    timelineVisible: false,
  },
  description: 'Attribute Definition resource.',
  key: 'commerce.catalog.attribute-definition',
  label: 'Attribute Definition',
  owningModuleId: 'commerce.catalog',
} as const satisfies OntosResourceType;
