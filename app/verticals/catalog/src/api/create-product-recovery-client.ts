// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { CreateProductRecoveryApi } from '../../shared/apis/create-product-recovery.ts';
import type { CreateProductRecoveryRequest } from '../../shared/apis/create-product-recovery.ts';
import { operationGateway } from './action-gateway.ts';

export interface CreateProductRecoveryClientOptions {
  readonly baseUrl?: string | URL;
}

type CreateProductRecoveryAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: CreateProductRecoveryClientOptions,
];

type CreateProductRecoveryOperationInvocation = readonly [
  requestCorrelation: string,
  options?: CreateProductRecoveryClientOptions,
];

const createProductRecoveryClient = (
  authorization: { readonly credential: Redacted.Redacted; readonly requestCorrelation: string },
  options: CreateProductRecoveryClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: CreateProductRecoveryApi,
      credential: authorization.credential,
      defaultApiPrefix: '/catalog-api',
      requestCorrelation: authorization.requestCorrelation,
    },
    options,
  );

export const executeCreateProductRecoveryWithAuthorization = (
  payload: CreateProductRecoveryRequest,
  ...[credential, requestCorrelation, options = {}]: CreateProductRecoveryAuthorizedInvocation
) =>
  createProductRecoveryClient({ credential: Redacted.make(credential), requestCorrelation }, options).pipe(
    Effect.flatMap((client) => client.createProductRecovery.execute({ headers: {}, params: {}, payload, query: {} })),
  );

export const executeCreateProductRecovery = (
  payload: CreateProductRecoveryRequest,
  ...[requestCorrelation, options = {}]: CreateProductRecoveryOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeCreateProductRecoveryWithAuthorization(payload, credential, requestCorrelation, options),
  );
