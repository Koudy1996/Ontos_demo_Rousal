// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { JobCommitStatusApi } from '../../shared/apis/job-commit-status.ts';
import type { JobCommitStatusRequest } from '../../shared/apis/job-commit-status.ts';
import { operationGateway } from './action-gateway.ts';

export interface JobCommitStatusClientOptions {
  readonly baseUrl?: string | URL;
}

type JobCommitStatusAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: JobCommitStatusClientOptions,
];

type JobCommitStatusOperationInvocation = readonly [requestCorrelation: string, options?: JobCommitStatusClientOptions];

const jobCommitStatusClient = (
  credential: Redacted.Redacted,
  requestCorrelation: string,
  options: JobCommitStatusClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: JobCommitStatusApi,
      credential,
      defaultApiPrefix: '/service-jobs-api',
      requestCorrelation,
    },
    options,
  );

export const executeJobCommitStatusWithAuthorization = (
  payload: JobCommitStatusRequest,
  ...[credential, requestCorrelation, options = {}]: JobCommitStatusAuthorizedInvocation
) =>
  jobCommitStatusClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) => client.jobCommitStatus.execute({ headers: {}, params: {}, payload, query: {} })),
  );

export const executeJobCommitStatus = (
  payload: JobCommitStatusRequest,
  ...[requestCorrelation, options = {}]: JobCommitStatusOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeJobCommitStatusWithAuthorization(payload, credential, requestCorrelation, options),
  );
