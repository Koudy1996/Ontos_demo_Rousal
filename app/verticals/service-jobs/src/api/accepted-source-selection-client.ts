// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { AcceptedSourceSelectionApi } from '../../shared/apis/accepted-source-selection.ts';
import type { AcceptedSourceSelectionRequest } from '../../shared/apis/accepted-source-selection.ts';
import { operationGateway } from './action-gateway.ts';

export interface AcceptedSourceSelectionClientOptions {
  readonly baseUrl?: string | URL;
}

type AcceptedSourceSelectionAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: AcceptedSourceSelectionClientOptions,
];

type AcceptedSourceSelectionOperationInvocation = readonly [
  requestCorrelation: string,
  options?: AcceptedSourceSelectionClientOptions,
];

const acceptedSourceSelectionClient = (
  credential: Redacted.Redacted,
  requestCorrelation: string,
  options: AcceptedSourceSelectionClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: AcceptedSourceSelectionApi,
      credential,
      defaultApiPrefix: '/service-jobs-api',
      requestCorrelation,
    },
    options,
  );

export const executeAcceptedSourceSelectionWithAuthorization = (
  payload: AcceptedSourceSelectionRequest,
  ...[credential, requestCorrelation, options = {}]: AcceptedSourceSelectionAuthorizedInvocation
) =>
  acceptedSourceSelectionClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) => client.acceptedSourceSelection.execute({ headers: {}, params: {}, payload, query: {} })),
  );

export const executeAcceptedSourceSelection = (
  payload: AcceptedSourceSelectionRequest,
  ...[requestCorrelation, options = {}]: AcceptedSourceSelectionOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeAcceptedSourceSelectionWithAuthorization(payload, credential, requestCorrelation, options),
  );
