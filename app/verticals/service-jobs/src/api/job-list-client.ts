// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { JobListApi } from '../../shared/apis/job-list.ts';
import type { JobListRequest } from '../../shared/apis/job-list.ts';
import { operationGateway } from './action-gateway.ts';

export interface JobListClientOptions {
  readonly baseUrl?: string | URL;
}

type JobListAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: JobListClientOptions,
];

type JobListOperationInvocation = readonly [requestCorrelation: string, options?: JobListClientOptions];

const jobListClient = (credential: Redacted.Redacted, requestCorrelation: string, options: JobListClientOptions) =>
  makeGovernedEffectBffClient(
    {
      api: JobListApi,
      credential,
      defaultApiPrefix: '/service-jobs-api',
      requestCorrelation,
    },
    options,
  );

export const executeJobListWithAuthorization = (
  payload: JobListRequest,
  ...[credential, requestCorrelation, options = {}]: JobListAuthorizedInvocation
) =>
  jobListClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) => client.jobList.execute({ headers: {}, params: {}, payload, query: {} })),
  );

export const executeJobList = (
  payload: JobListRequest,
  ...[requestCorrelation, options = {}]: JobListOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeJobListWithAuthorization(payload, credential, requestCorrelation, options),
  );
