// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { JobExpensesCommitStatusApi } from '../../shared/apis/job-expenses-commit-status.ts';
import type { JobExpensesCommitStatusRequest } from '../../shared/apis/job-expenses-commit-status.ts';
import { operationGateway } from './action-gateway.ts';

export interface JobExpensesCommitStatusClientOptions {
  readonly baseUrl?: string | URL;
}

type JobExpensesCommitStatusAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: JobExpensesCommitStatusClientOptions,
];

type JobExpensesCommitStatusOperationInvocation = readonly [
  requestCorrelation: string,
  options?: JobExpensesCommitStatusClientOptions,
];

const jobExpensesCommitStatusClient = (
  credential: Redacted.Redacted,
  requestCorrelation: string,
  options: JobExpensesCommitStatusClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: JobExpensesCommitStatusApi,
      credential,
      defaultApiPrefix: '/job-expenses-api',
      requestCorrelation,
    },
    options,
  );

export const executeJobExpensesCommitStatusWithAuthorization = (
  payload: JobExpensesCommitStatusRequest,
  ...[credential, requestCorrelation, options = {}]: JobExpensesCommitStatusAuthorizedInvocation
) =>
  jobExpensesCommitStatusClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) => client.jobExpensesCommitStatus.execute({ headers: {}, params: {}, payload, query: {} })),
  );

export const executeJobExpensesCommitStatus = (
  payload: JobExpensesCommitStatusRequest,
  ...[requestCorrelation, options = {}]: JobExpensesCommitStatusOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeJobExpensesCommitStatusWithAuthorization(payload, credential, requestCorrelation, options),
  );
