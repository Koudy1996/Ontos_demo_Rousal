// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { JobExpenseListApi } from '../../shared/apis/job-expense-list.ts';
import type { JobExpenseListRequest } from '../../shared/apis/job-expense-list.ts';
import { operationGateway } from './action-gateway.ts';

export interface JobExpenseListClientOptions {
  readonly baseUrl?: string | URL;
}

type JobExpenseListAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: JobExpenseListClientOptions,
];

type JobExpenseListOperationInvocation = readonly [requestCorrelation: string, options?: JobExpenseListClientOptions];

const jobExpenseListClient = (
  credential: Redacted.Redacted,
  requestCorrelation: string,
  options: JobExpenseListClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: JobExpenseListApi,
      credential,
      defaultApiPrefix: '/job-expenses-api',
      requestCorrelation,
    },
    options,
  );

export const executeJobExpenseListWithAuthorization = (
  payload: JobExpenseListRequest,
  ...[credential, requestCorrelation, options = {}]: JobExpenseListAuthorizedInvocation
) =>
  jobExpenseListClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) => client.jobExpenseList.execute({ headers: {}, params: {}, payload, query: {} })),
  );

export const executeJobExpenseList = (
  payload: JobExpenseListRequest,
  ...[requestCorrelation, options = {}]: JobExpenseListOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeJobExpenseListWithAuthorization(payload, credential, requestCorrelation, options),
  );
