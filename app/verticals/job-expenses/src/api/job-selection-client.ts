// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { JobSelectionApi } from '../../shared/apis/job-selection.ts';
import type { JobSelectionRequest } from '../../shared/apis/job-selection.ts';
import { operationGateway } from './action-gateway.ts';

export interface JobSelectionClientOptions {
  readonly baseUrl?: string | URL;
}

type JobSelectionAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: JobSelectionClientOptions,
];

type JobSelectionOperationInvocation = readonly [requestCorrelation: string, options?: JobSelectionClientOptions];

const jobSelectionClient = (
  credential: Redacted.Redacted,
  requestCorrelation: string,
  options: JobSelectionClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: JobSelectionApi,
      credential,
      defaultApiPrefix: '/job-expenses-api',
      requestCorrelation,
    },
    options,
  );

export const executeJobSelectionWithAuthorization = (
  payload: JobSelectionRequest,
  ...[credential, requestCorrelation, options = {}]: JobSelectionAuthorizedInvocation
) =>
  jobSelectionClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) => client.jobSelection.execute({ headers: {}, params: {}, payload, query: {} })),
  );

export const executeJobSelection = (
  payload: JobSelectionRequest,
  ...[requestCorrelation, options = {}]: JobSelectionOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeJobSelectionWithAuthorization(payload, credential, requestCorrelation, options),
  );
