// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { InvoiceDraftSupportApi } from '../../shared/apis/invoice-draft-support.ts';
import type { InvoiceDraftSupportRequest } from '../../shared/apis/invoice-draft-support.ts';
import { operationGateway } from './action-gateway.ts';

export interface InvoiceDraftSupportClientOptions {
  readonly baseUrl?: string | URL;
}

type InvoiceDraftSupportAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: InvoiceDraftSupportClientOptions,
];

type InvoiceDraftSupportOperationInvocation = readonly [
  requestCorrelation: string,
  options?: InvoiceDraftSupportClientOptions,
];

const invoiceDraftSupportClient = (
  credential: Redacted.Redacted,
  requestCorrelation: string,
  options: InvoiceDraftSupportClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: InvoiceDraftSupportApi,
      credential,
      defaultApiPrefix: '/billing-documents-api',
      requestCorrelation,
    },
    options,
  );

export const executeInvoiceDraftSupportWithAuthorization = (
  payload: InvoiceDraftSupportRequest,
  ...[credential, requestCorrelation, options = {}]: InvoiceDraftSupportAuthorizedInvocation
) =>
  invoiceDraftSupportClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) => client.invoiceDraftSupport.execute({ headers: {}, params: {}, payload, query: {} })),
  );

export const executeInvoiceDraftSupport = (
  payload: InvoiceDraftSupportRequest,
  ...[requestCorrelation, options = {}]: InvoiceDraftSupportOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeInvoiceDraftSupportWithAuthorization(payload, credential, requestCorrelation, options),
  );
