// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { InvoiceListApi } from '../../shared/apis/invoice-list.ts';
import type { InvoiceListRequest } from '../../shared/apis/invoice-list.ts';
import { operationGateway } from './action-gateway.ts';

export interface InvoiceListClientOptions {
  readonly baseUrl?: string | URL;
}

type InvoiceListAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: InvoiceListClientOptions,
];

type InvoiceListOperationInvocation = readonly [requestCorrelation: string, options?: InvoiceListClientOptions];

const invoiceListClient = (
  credential: Redacted.Redacted,
  requestCorrelation: string,
  options: InvoiceListClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: InvoiceListApi,
      credential,
      defaultApiPrefix: '/billing-documents-api',
      requestCorrelation,
    },
    options,
  );

export const executeInvoiceListWithAuthorization = (
  payload: InvoiceListRequest,
  ...[credential, requestCorrelation, options = {}]: InvoiceListAuthorizedInvocation
) =>
  invoiceListClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) => client.invoiceList.execute({ headers: {}, params: {}, payload, query: {} })),
  );

export const executeInvoiceList = (
  payload: InvoiceListRequest,
  ...[requestCorrelation, options = {}]: InvoiceListOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeInvoiceListWithAuthorization(payload, credential, requestCorrelation, options),
  );
