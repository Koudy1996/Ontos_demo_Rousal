// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { BillingDocumentsCommitStatusApi } from '../../shared/apis/billing-documents-commit-status.ts';
import type { BillingDocumentsCommitStatusRequest } from '../../shared/apis/billing-documents-commit-status.ts';
import { operationGateway } from './action-gateway.ts';

export interface BillingDocumentsCommitStatusClientOptions {
  readonly baseUrl?: string | URL;
}

type BillingDocumentsCommitStatusAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: BillingDocumentsCommitStatusClientOptions,
];

type BillingDocumentsCommitStatusOperationInvocation = readonly [
  requestCorrelation: string,
  options?: BillingDocumentsCommitStatusClientOptions,
];

const billingDocumentsCommitStatusClient = (
  credential: Redacted.Redacted,
  requestCorrelation: string,
  options: BillingDocumentsCommitStatusClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: BillingDocumentsCommitStatusApi,
      credential,
      defaultApiPrefix: '/billing-documents-api',
      requestCorrelation,
    },
    options,
  );

export const executeBillingDocumentsCommitStatusWithAuthorization = (
  payload: BillingDocumentsCommitStatusRequest,
  ...[credential, requestCorrelation, options = {}]: BillingDocumentsCommitStatusAuthorizedInvocation
) =>
  billingDocumentsCommitStatusClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) =>
      client.billingDocumentsCommitStatus.execute({ headers: {}, params: {}, payload, query: {} }),
    ),
  );

export const executeBillingDocumentsCommitStatus = (
  payload: BillingDocumentsCommitStatusRequest,
  ...[requestCorrelation, options = {}]: BillingDocumentsCommitStatusOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeBillingDocumentsCommitStatusWithAuthorization(payload, credential, requestCorrelation, options),
  );
