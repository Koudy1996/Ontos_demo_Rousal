// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner billing.documents
// @ontos-action-http-slug issue-invoice
// oxlint-disable sonarjs/function-name -- Effect Match.tags requires owner-declared tag keys; remove-when: sonarjs accepts discriminant-map properties.
import type { ActionCoreError } from '@app/core-runtime';
import { Effect, HttpApiMiddleware } from '@modern-js/bff-effect/effect-edge';
import { Match, Schema } from 'effect';
import {
  IssueInvoiceActionAlreadyCommittedProblemSchema,
  IssueInvoiceActionAuthenticationProblemSchema,
  IssueInvoiceActionCommitIndeterminateProblemSchema,
  IssueInvoiceActionConflictProblemSchema,
  IssueInvoiceActionForbiddenProblemSchema,
  IssueInvoiceActionIneligibleProblemSchema,
  IssueInvoiceActionInternalProblemSchema,
  IssueInvoiceActionInvalidProblemSchema,
  IssueInvoiceActionNotFoundProblemSchema,
  IssueInvoiceActionPreconditionProblemSchema,
  IssueInvoiceActionSchemaErrorMiddleware,
  IssueInvoiceActionUnavailableProblemSchema,
} from '../shared/apis/issue-invoice-action.ts';
import type { IssueInvoiceActionProblem } from '../shared/apis/issue-invoice-action.ts';
import { issueInvoiceAction } from '../src/actions/issue-invoice.action.ts';

type DomainError = typeof issueInvoiceAction.descriptor.domainErrorSchema.Type;
type ProblemOf<Tag extends IssueInvoiceActionProblem['_tag']> = Extract<
  IssueInvoiceActionProblem,
  { readonly _tag: Tag }
>;
type DomainProblemIdentity =
  | { readonly code: 'invoice_already_exists' | 'revision_conflict'; readonly kind: 'conflict' }
  | { readonly code: 'scope_mismatch'; readonly kind: 'forbidden' }
  | {
      readonly code:
        | 'address_not_current'
        | 'due_date_out_of_range'
        | 'invoice_not_draft'
        | 'job_not_completed'
        | 'payment_term_required'
        | 'payment_term_unusable'
        | 'source_job_changed'
        | 'tax_data_required';
      readonly kind: 'ineligible';
    }
  | { readonly code: 'job_not_found' | 'missing_billing_address' | 'not_found'; readonly kind: 'notFound' }
  | { readonly code: 'billing_documents_unavailable'; readonly kind: 'unavailable' };

const problemStatus = {
  authentication: 401,
  conflict: 409,
  forbidden: 403,
  ineligible: 422,
  internal: 500,
  invalid: 400,
  notFound: 404,
  precondition: 428,
  rateLimited: 429,
  timeout: 504,
  unavailable: 503,
} as const;

const invoiceRejectedProblemByCode = {
  address_not_current: { code: 'address_not_current', kind: 'ineligible' },
  due_date_out_of_range: { code: 'due_date_out_of_range', kind: 'ineligible' },
  invoice_already_exists: { code: 'invoice_already_exists', kind: 'conflict' },
  invoice_not_draft: { code: 'invoice_not_draft', kind: 'ineligible' },
  job_not_completed: { code: 'job_not_completed', kind: 'ineligible' },
  job_not_found: { code: 'job_not_found', kind: 'notFound' },
  missing_billing_address: { code: 'missing_billing_address', kind: 'notFound' },
  not_found: { code: 'not_found', kind: 'notFound' },
  payment_term_required: { code: 'payment_term_required', kind: 'ineligible' },
  payment_term_unusable: { code: 'payment_term_unusable', kind: 'ineligible' },
  revision_conflict: { code: 'revision_conflict', kind: 'conflict' },
  scope_mismatch: { code: 'scope_mismatch', kind: 'forbidden' },
  source_job_changed: { code: 'source_job_changed', kind: 'ineligible' },
  tax_data_required: { code: 'tax_data_required', kind: 'ineligible' },
} as const satisfies Record<Extract<DomainError, { readonly _tag: 'InvoiceRejected' }>['code'], DomainProblemIdentity>;

export const issueInvoiceActionProblem = {
  authentication: (): ProblemOf<'IssueInvoiceActionAuthenticationProblem'> =>
    IssueInvoiceActionAuthenticationProblemSchema.make({
      detail: 'A valid audience-scoped Bearer assertion is required.',
      status: problemStatus.authentication,
      title: 'Authentication required',
      type: 'https://ontos.dev/problems/operation-authentication-required',
    }),
  conflict: (
    code: ProblemOf<'IssueInvoiceActionConflictProblem'>['code'],
  ): ProblemOf<'IssueInvoiceActionConflictProblem'> =>
    IssueInvoiceActionConflictProblemSchema.make({
      code,
      detail: 'The Action conflicts with current state.',
      status: problemStatus.conflict,
      title: 'Action conflict',
      type: 'https://ontos.dev/problems/action-conflict',
    }),
  forbidden: (
    code: ProblemOf<'IssueInvoiceActionForbiddenProblem'>['code'],
  ): ProblemOf<'IssueInvoiceActionForbiddenProblem'> =>
    IssueInvoiceActionForbiddenProblemSchema.make({
      code,
      detail: 'The principal is not permitted to perform this Action.',
      status: problemStatus.forbidden,
      title: 'Action forbidden',
      type: 'https://ontos.dev/problems/action-forbidden',
    }),
  ineligible: (
    code: ProblemOf<'IssueInvoiceActionIneligibleProblem'>['code'],
  ): ProblemOf<'IssueInvoiceActionIneligibleProblem'> =>
    IssueInvoiceActionIneligibleProblemSchema.make({
      code,
      detail: 'The request is not eligible for this Action.',
      status: problemStatus.ineligible,
      title: 'Action ineligible',
      type: 'https://ontos.dev/problems/action-ineligible',
    }),
  internal: (): ProblemOf<'IssueInvoiceActionInternalProblem'> =>
    IssueInvoiceActionInternalProblemSchema.make({
      detail: 'The Action could not be completed.',
      status: problemStatus.internal,
      title: 'Action failed',
      type: 'https://ontos.dev/problems/action-failed',
    }),
  invalid: (): ProblemOf<'IssueInvoiceActionInvalidProblem'> =>
    IssueInvoiceActionInvalidProblemSchema.make({
      detail: 'The issue-invoice Action request is invalid.',
      status: problemStatus.invalid,
      title: 'Invalid Action request',
      type: 'https://ontos.dev/problems/action-invalid',
    }),
  notFound: (
    code: ProblemOf<'IssueInvoiceActionNotFoundProblem'>['code'],
  ): ProblemOf<'IssueInvoiceActionNotFoundProblem'> =>
    IssueInvoiceActionNotFoundProblemSchema.make({
      code,
      detail: 'The requested resource was not found.',
      status: problemStatus.notFound,
      title: 'Resource not found',
      type: 'https://ontos.dev/problems/action-resource-not-found',
    }),
  precondition: (): ProblemOf<'IssueInvoiceActionPreconditionProblem'> =>
    IssueInvoiceActionPreconditionProblemSchema.make({
      detail: 'An Idempotency-Key header is required.',
      status: problemStatus.precondition,
      title: 'Idempotency key required',
      type: 'https://ontos.dev/problems/idempotency-key-required',
    }),
  unavailable: (
    code: ProblemOf<'IssueInvoiceActionUnavailableProblem'>['code'],
  ): ProblemOf<'IssueInvoiceActionUnavailableProblem'> =>
    IssueInvoiceActionUnavailableProblemSchema.make({
      code,
      detail: 'The Action capability is temporarily unavailable.',
      retryable: true,
      status: problemStatus.unavailable,
      title: 'Action unavailable',
      type: 'https://ontos.dev/problems/action-unavailable',
    }),
} as const;

const mapDomainIdentity = (identity: DomainProblemIdentity): IssueInvoiceActionProblem =>
  Match.value(identity).pipe(
    Match.when({ kind: 'conflict' as const }, (matched) => issueInvoiceActionProblem.conflict(matched.code)),
    Match.when({ kind: 'forbidden' as const }, (matched) => issueInvoiceActionProblem.forbidden(matched.code)),
    Match.when({ kind: 'ineligible' as const }, (matched) => issueInvoiceActionProblem.ineligible(matched.code)),
    Match.when({ kind: 'notFound' as const }, (matched) => issueInvoiceActionProblem.notFound(matched.code)),
    Match.when({ kind: 'unavailable' as const }, (matched) => issueInvoiceActionProblem.unavailable(matched.code)),
    Match.exhaustive,
  );

const mapDomainProblem = (error: DomainError): IssueInvoiceActionProblem =>
  Match.value(error).pipe(
    Match.tags({
      InvoiceRejected: (failure) => mapDomainIdentity(invoiceRejectedProblemByCode[failure.code]),
      InvoiceUnavailable: () => issueInvoiceActionProblem.unavailable('billing_documents_unavailable'),
    }),
    Match.exhaustive,
  );

const mapCoreProblem = (error: ActionCoreError): IssueInvoiceActionProblem =>
  Match.value(error).pipe(
    Match.tags({
      ActionAlreadyCommitted: (failure) =>
        IssueInvoiceActionAlreadyCommittedProblemSchema.make({
          code: failure.code,
          detail: 'This Action is already committed. Refresh governed reads.',
          invocationId: failure.invocationId,
          resolution: 'REFRESH_GOVERNED_READS',
          retryCommand: false,
          status: problemStatus.conflict,
          title: 'Action already committed',
          type: 'https://ontos.dev/problems/action-already-committed',
        }),
      ActionCollectorError: issueInvoiceActionProblem.internal,
      ActionCommitIndeterminate: (failure) =>
        IssueInvoiceActionCommitIndeterminateProblemSchema.make({
          detail: 'The Action commit is uncertain. Resolve this invocation before another attempt.',
          invocationId: failure.invocationId,
          resolution: 'RESOLVE_COMMIT',
          retryCommand: false,
          status: problemStatus.unavailable,
          title: 'Action commit uncertain',
          type: 'https://ontos.dev/problems/action-commit-indeterminate',
        }),
      ActionHandlerExecutionError: issueInvoiceActionProblem.internal,
      ActionIdempotencyKeyRequired: issueInvoiceActionProblem.precondition,
      ActionInvocationNotFound: (failure) => issueInvoiceActionProblem.notFound(failure.code),
      ActionInvocationPersistenceError: (failure) => issueInvoiceActionProblem.unavailable(failure.code),
      ActionInvocationStateError: (failure) => issueInvoiceActionProblem.conflict(failure.code),
      ActionPayloadValidationError: issueInvoiceActionProblem.invalid,
      ActionPermissionCheckError: (failure) => issueInvoiceActionProblem.unavailable(failure.code),
      ActionPermissionDenied: (failure) => issueInvoiceActionProblem.forbidden(failure.code),
      ActionPolicyDenied: (failure) => issueInvoiceActionProblem.ineligible(failure.code),
      ActionPolicyEvaluationError: (failure) => issueInvoiceActionProblem.unavailable(failure.code),
      ActionRequestHashConflict: (failure) => issueInvoiceActionProblem.conflict(failure.code),
      ActionResultValidationError: issueInvoiceActionProblem.internal,
      ActionTransactionError: (failure) => issueInvoiceActionProblem.unavailable(failure.code),
      ActionTrustedContextValidationError: issueInvoiceActionProblem.authentication,
      ModuleStateCheckUnavailableError: (failure) => issueInvoiceActionProblem.unavailable(failure.code),
      ModuleStateDeniedError: (failure) => issueInvoiceActionProblem.forbidden(failure.code),
      OperationAuthenticationRequired: issueInvoiceActionProblem.authentication,
      OperationContextDenied: (failure) => issueInvoiceActionProblem.forbidden(failure.code),
      OperationContextInvalid: (failure) => issueInvoiceActionProblem.forbidden(failure.code),
      OperationContextUnavailable: (failure) => issueInvoiceActionProblem.unavailable(failure.code),
    }),
    Match.exhaustive,
  );

const isDomainError = Schema.is(issueInvoiceAction.descriptor.domainErrorSchema);
export const mapIssueInvoiceActionProblem = (error: ActionCoreError | DomainError): IssueInvoiceActionProblem =>
  isDomainError(error) ? mapDomainProblem(error) : mapCoreProblem(error);

export const issueInvoiceActionSchemaErrorLive = HttpApiMiddleware.layerSchemaErrorTransform(
  IssueInvoiceActionSchemaErrorMiddleware,
  () => Effect.fail(issueInvoiceActionProblem.invalid()),
);
