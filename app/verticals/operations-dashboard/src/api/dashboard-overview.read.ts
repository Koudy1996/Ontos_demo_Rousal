// @generated by OntOS Codesmith module-api v1
import { defineRead, defineTenantModuleEntrypoint } from '@app/core-runtime';
import type { ReadHandlerContext } from '@app/core-runtime';
import { Effect } from 'effect';
import {
  DashboardOverviewRequestSchema,
  DashboardOverviewResponseSchema,
} from '../../shared/apis/dashboard-overview.ts';
import { dashboardOverviewService } from '../services/dashboard-overview.service.ts';
import type { DashboardOverviewServices } from '../services/dashboard-overview.service.ts';

const operationsDashboardModuleId = 'operations.dashboard' as const;
const dashboardOverviewReadKey = 'operations.dashboard.api.dashboard-overview' as const;
const dashboardOverviewEntrypoint = defineTenantModuleEntrypoint({
  access: 'read',
  authorization: { kind: 'context_permission', permission: 'module.access' },
  entrypointKey: dashboardOverviewReadKey,
  moduleKey: operationsDashboardModuleId,
  role: 'api',
});

export const dashboardOverviewRead = defineRead(
  {
    accessKind: 'detail',
    entrypoint: dashboardOverviewEntrypoint,
    evidencePolicy: {
      captureMode: 'metadata_only',
      policyKey: 'operations.dashboard.api.dashboard-overview.evidence.v1',
    },
    inputSchema: DashboardOverviewRequestSchema,
    legalEntityScope: 'required',
    owningModuleKey: operationsDashboardModuleId,
    permissionTarget: 'module',
    policies: [],
    readKey: dashboardOverviewReadKey,
    resultSchema: DashboardOverviewResponseSchema,
    schemaVersion: '1',
  },
  (_input, context: ReadHandlerContext<DashboardOverviewServices>) =>
    context.services.load.pipe(Effect.map((result) => ({ evidence: { resultCount: 1 }, result }))),
  dashboardOverviewService,
  () => ({ kind: 'module', moduleId: operationsDashboardModuleId }),
);
