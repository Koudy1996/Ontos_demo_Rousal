// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { DashboardOverviewApi } from '../../shared/apis/dashboard-overview.ts';
import type { DashboardOverviewRequest } from '../../shared/apis/dashboard-overview.ts';
import { operationGateway } from './action-gateway.ts';

export interface DashboardOverviewClientOptions {
  readonly baseUrl?: string | URL;
}

type DashboardOverviewAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: DashboardOverviewClientOptions,
];

type DashboardOverviewOperationInvocation = readonly [
  requestCorrelation: string,
  options?: DashboardOverviewClientOptions,
];

const dashboardOverviewClient = (
  credential: Redacted.Redacted,
  requestCorrelation: string,
  options: DashboardOverviewClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: DashboardOverviewApi,
      credential,
      defaultApiPrefix: '/operations-dashboard-api',
      requestCorrelation,
    },
    options,
  );

export const executeDashboardOverviewWithAuthorization = (
  payload: DashboardOverviewRequest,
  ...[credential, requestCorrelation, options = {}]: DashboardOverviewAuthorizedInvocation
) =>
  dashboardOverviewClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) => client.dashboardOverview.execute({ headers: {}, params: {}, payload, query: {} })),
  );

export const executeDashboardOverview = (
  payload: DashboardOverviewRequest,
  ...[requestCorrelation, options = {}]: DashboardOverviewOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeDashboardOverviewWithAuthorization(payload, credential, requestCorrelation, options),
  );
