// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner sales.inquiries
// @ontos-action-http-slug update-offer-draft
import { Effect, HttpApiBuilder, Layer } from '@modern-js/bff-effect/effect-edge';
import type { HttpServerRequest } from 'effect/unstable/http';
import { Redacted, Schema } from 'effect';
import { salesInquiriesApi } from '../shared/api.ts';
import { updateOfferDraftAction } from '../src/actions/update-offer-draft.action.ts';
import { bindActionHttpRunner } from './action-http-runner.ts';
import {
  updateOfferDraftActionProblem,
  updateOfferDraftActionSchemaErrorLive,
  mapUpdateOfferDraftActionProblem,
} from './update-offer-draft-action-problems.ts';

const runActionHttp = bindActionHttpRunner({
  authentication: updateOfferDraftActionProblem.authentication,
  unavailable: () => updateOfferDraftActionProblem.unavailable('operation_context_unavailable'),
});

const execute = Effect.fn('UpdateOfferDraftActionServer.execute')(function* executeUpdateOfferDraft(
  payload: typeof updateOfferDraftAction.descriptor.payloadSchema.Encoded,
  idempotencyKey: string | undefined,
  request: HttpServerRequest.HttpServerRequest,
) {
  const correlationId = request.headers['x-correlation-id'];
  if (correlationId !== undefined && correlationId.length > 200) {
    return yield* Effect.fail(updateOfferDraftActionProblem.invalid());
  }
  const decoded = yield* Schema.decodeEffect(updateOfferDraftAction.descriptor.payloadSchema)(payload).pipe(
    Effect.mapError(updateOfferDraftActionProblem.invalid),
  );
  return yield* runActionHttp({
    endpointHeaders: { idempotencyKey, traceId: request.headers['x-trace-id'] },
    internalProblem: updateOfferDraftActionProblem.internal,
    invalidCorrelationProblem: updateOfferDraftActionProblem.invalid,
    mapError: mapUpdateOfferDraftActionProblem,
    payload: decoded,
    registration: updateOfferDraftAction,
    requestHeaders: {
      authorization: Redacted.make(request.headers['authorization']),
      'x-correlation-id': correlationId,
    },
  });
});

export const updateOfferDraftActionApiLive = HttpApiBuilder.group(
  salesInquiriesApi,
  'updateOfferDraftAction',
  (handlers) =>
    handlers.handle('execute', ({ headers, payload, request }) =>
      execute(payload, headers['idempotency-key'], request),
    ),
).pipe(Layer.provide(updateOfferDraftActionSchemaErrorLive));
