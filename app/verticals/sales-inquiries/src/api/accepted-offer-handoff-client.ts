// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { AcceptedOfferHandoffApi } from '../../shared/apis/accepted-offer-handoff.ts';
import type { AcceptedOfferHandoffRequest } from '../../shared/apis/accepted-offer-handoff.ts';
import { operationGateway } from './action-gateway.ts';

export interface AcceptedOfferHandoffClientOptions {
  readonly baseUrl?: string | URL;
}

type AcceptedOfferHandoffAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: AcceptedOfferHandoffClientOptions,
];

type AcceptedOfferHandoffOperationInvocation = readonly [
  requestCorrelation: string,
  options?: AcceptedOfferHandoffClientOptions,
];

const acceptedOfferHandoffClient = (
  credential: Redacted.Redacted,
  requestCorrelation: string,
  options: AcceptedOfferHandoffClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: AcceptedOfferHandoffApi,
      credential,
      defaultApiPrefix: '/sales-inquiries-api',
      requestCorrelation,
    },
    options,
  );

export const executeAcceptedOfferHandoffWithAuthorization = (
  payload: AcceptedOfferHandoffRequest,
  ...[credential, requestCorrelation, options = {}]: AcceptedOfferHandoffAuthorizedInvocation
) =>
  acceptedOfferHandoffClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) => client.acceptedOfferHandoff.execute({ headers: {}, params: {}, payload, query: {} })),
  );

export const executeAcceptedOfferHandoff = (
  payload: AcceptedOfferHandoffRequest,
  ...[requestCorrelation, options = {}]: AcceptedOfferHandoffOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeAcceptedOfferHandoffWithAuthorization(payload, credential, requestCorrelation, options),
  );
