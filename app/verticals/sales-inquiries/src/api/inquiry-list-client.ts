// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { InquiryListApi } from '../../shared/apis/inquiry-list.ts';
import type { InquiryListRequest } from '../../shared/apis/inquiry-list.ts';
import { operationGateway } from './action-gateway.ts';

export interface InquiryListClientOptions {
  readonly baseUrl?: string | URL;
}

type InquiryListAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: InquiryListClientOptions,
];

type InquiryListOperationInvocation = readonly [requestCorrelation: string, options?: InquiryListClientOptions];

const inquiryListClient = (
  credential: Redacted.Redacted,
  requestCorrelation: string,
  options: InquiryListClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: InquiryListApi,
      credential,
      defaultApiPrefix: '/sales-inquiries-api',
      requestCorrelation,
    },
    options,
  );

export const executeInquiryListWithAuthorization = (
  payload: InquiryListRequest,
  ...[credential, requestCorrelation, options = {}]: InquiryListAuthorizedInvocation
) =>
  inquiryListClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) => client.inquiryList.execute({ headers: {}, params: {}, payload, query: {} })),
  );

export const executeInquiryList = (
  payload: InquiryListRequest,
  ...[requestCorrelation, options = {}]: InquiryListOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeInquiryListWithAuthorization(payload, credential, requestCorrelation, options),
  );
