// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { InquiryCommitStatusApi } from '../../shared/apis/inquiry-commit-status.ts';
import type { InquiryCommitStatusRequest } from '../../shared/apis/inquiry-commit-status.ts';
import { operationGateway } from './action-gateway.ts';

export interface InquiryCommitStatusClientOptions {
  readonly baseUrl?: string | URL;
}

type InquiryCommitStatusAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: InquiryCommitStatusClientOptions,
];

type InquiryCommitStatusOperationInvocation = readonly [
  requestCorrelation: string,
  options?: InquiryCommitStatusClientOptions,
];

const inquiryCommitStatusClient = (
  credential: Redacted.Redacted,
  requestCorrelation: string,
  options: InquiryCommitStatusClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: InquiryCommitStatusApi,
      credential,
      defaultApiPrefix: '/sales-inquiries-api',
      requestCorrelation,
    },
    options,
  );

export const executeInquiryCommitStatusWithAuthorization = (
  payload: InquiryCommitStatusRequest,
  ...[credential, requestCorrelation, options = {}]: InquiryCommitStatusAuthorizedInvocation
) =>
  inquiryCommitStatusClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) => client.inquiryCommitStatus.execute({ headers: {}, params: {}, payload, query: {} })),
  );

export const executeInquiryCommitStatus = (
  payload: InquiryCommitStatusRequest,
  ...[requestCorrelation, options = {}]: InquiryCommitStatusOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeInquiryCommitStatusWithAuthorization(payload, credential, requestCorrelation, options),
  );
