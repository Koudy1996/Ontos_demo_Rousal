// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { AvailableWorkersApi } from '../../shared/apis/available-workers.ts';
import type { AvailableWorkersRequest } from '../../shared/apis/available-workers.ts';
import { operationGateway } from './action-gateway.ts';

export interface AvailableWorkersClientOptions {
  readonly baseUrl?: string | URL;
}

type AvailableWorkersAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: AvailableWorkersClientOptions,
];

type AvailableWorkersOperationInvocation = readonly [
  requestCorrelation: string,
  options?: AvailableWorkersClientOptions,
];

const availableWorkersClient = (
  credential: Redacted.Redacted,
  requestCorrelation: string,
  options: AvailableWorkersClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: AvailableWorkersApi,
      credential,
      defaultApiPrefix: '/workforce-api',
      requestCorrelation,
    },
    options,
  );

export const executeAvailableWorkersWithAuthorization = (
  payload: AvailableWorkersRequest,
  ...[credential, requestCorrelation, options = {}]: AvailableWorkersAuthorizedInvocation
) =>
  availableWorkersClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) => client.availableWorkers.execute({ headers: {}, params: {}, payload, query: {} })),
  );

export const executeAvailableWorkers = (
  payload: AvailableWorkersRequest,
  ...[requestCorrelation, options = {}]: AvailableWorkersOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeAvailableWorkersWithAuthorization(payload, credential, requestCorrelation, options),
  );
