// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { WorkerListApi } from '../../shared/apis/worker-list.ts';
import type { WorkerListRequest } from '../../shared/apis/worker-list.ts';
import { operationGateway } from './action-gateway.ts';

export interface WorkerListClientOptions {
  readonly baseUrl?: string | URL;
}

type WorkerListAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: WorkerListClientOptions,
];

type WorkerListOperationInvocation = readonly [requestCorrelation: string, options?: WorkerListClientOptions];

const workerListClient = (
  credential: Redacted.Redacted,
  requestCorrelation: string,
  options: WorkerListClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: WorkerListApi,
      credential,
      defaultApiPrefix: '/workforce-api',
      requestCorrelation,
    },
    options,
  );

export const executeWorkerListWithAuthorization = (
  payload: WorkerListRequest,
  ...[credential, requestCorrelation, options = {}]: WorkerListAuthorizedInvocation
) =>
  workerListClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) => client.workerList.execute({ headers: {}, params: {}, payload, query: {} })),
  );

export const executeWorkerList = (
  payload: WorkerListRequest,
  ...[requestCorrelation, options = {}]: WorkerListOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeWorkerListWithAuthorization(payload, credential, requestCorrelation, options),
  );
