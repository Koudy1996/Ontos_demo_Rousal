// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { WeeklyScheduleApi } from '../../shared/apis/weekly-schedule.ts';
import type { WeeklyScheduleRequest } from '../../shared/apis/weekly-schedule.ts';
import { operationGateway } from './action-gateway.ts';

export interface WeeklyScheduleClientOptions {
  readonly baseUrl?: string | URL;
}

type WeeklyScheduleAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: WeeklyScheduleClientOptions,
];

type WeeklyScheduleOperationInvocation = readonly [requestCorrelation: string, options?: WeeklyScheduleClientOptions];

const weeklyScheduleClient = (
  credential: Redacted.Redacted,
  requestCorrelation: string,
  options: WeeklyScheduleClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: WeeklyScheduleApi,
      credential,
      defaultApiPrefix: '/workforce-api',
      requestCorrelation,
    },
    options,
  );

export const executeWeeklyScheduleWithAuthorization = (
  payload: WeeklyScheduleRequest,
  ...[credential, requestCorrelation, options = {}]: WeeklyScheduleAuthorizedInvocation
) =>
  weeklyScheduleClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) => client.weeklySchedule.execute({ headers: {}, params: {}, payload, query: {} })),
  );

export const executeWeeklySchedule = (
  payload: WeeklyScheduleRequest,
  ...[requestCorrelation, options = {}]: WeeklyScheduleOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeWeeklyScheduleWithAuthorization(payload, credential, requestCorrelation, options),
  );
