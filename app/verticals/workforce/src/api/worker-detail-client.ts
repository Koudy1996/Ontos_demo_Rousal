// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { WorkerDetailApi } from '../../shared/apis/worker-detail.ts';
import type { WorkerDetailRequest } from '../../shared/apis/worker-detail.ts';
import { operationGateway } from './action-gateway.ts';

export interface WorkerDetailClientOptions {
  readonly baseUrl?: string | URL;
}

type WorkerDetailAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: WorkerDetailClientOptions,
];

type WorkerDetailOperationInvocation = readonly [requestCorrelation: string, options?: WorkerDetailClientOptions];

const workerDetailClient = (
  credential: Redacted.Redacted,
  requestCorrelation: string,
  options: WorkerDetailClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: WorkerDetailApi,
      credential,
      defaultApiPrefix: '/workforce-api',
      requestCorrelation,
    },
    options,
  );

export const executeWorkerDetailWithAuthorization = (
  payload: WorkerDetailRequest,
  ...[credential, requestCorrelation, options = {}]: WorkerDetailAuthorizedInvocation
) =>
  workerDetailClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) => client.workerDetail.execute({ headers: {}, params: {}, payload, query: {} })),
  );

export const executeWorkerDetail = (
  payload: WorkerDetailRequest,
  ...[requestCorrelation, options = {}]: WorkerDetailOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeWorkerDetailWithAuthorization(payload, credential, requestCorrelation, options),
  );
